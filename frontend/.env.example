# Base URL for the Flask backend.
# In docker-compose the nginx proxy forwards /api → backend, so leave this blank
# and the frontend will call same-origin paths. For local CRA dev with `npm start`,
# leave blank to use CRA's `proxy` setting in package.json.
REACT_APP_API_BASE_URL=
